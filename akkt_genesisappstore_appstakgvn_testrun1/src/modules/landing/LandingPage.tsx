import Hero from "./components/Hero";

export default function LandingPage() {
  return (
    <div className="min-h-screen">
      <Hero />
      {/* Add more sections like <Features />, <CTA />, etc. */}
    </div>
  );
}
